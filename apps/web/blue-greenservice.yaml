apiVersion: v1
kind: Service
metadata:
  name: web-svc
  namespace: web-blue
spec:
  type: NodePort   # LoadBalancer in cloud
  selector:
    app: web
    version: blue
  ports:
  - port: 80
    targetPort: 80

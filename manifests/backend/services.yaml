apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort   # LoadBalancer in cloud
  selector:
    app: frontend
    #version: blue
  ports:
  - port: 80
    targetPort: 80

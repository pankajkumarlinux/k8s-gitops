apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: NodePort   # LoadBalancer in cloud
  selector:
    app: backend
    #version: blue
  ports:
  - port: 80
    targetPort: 80
